import { MainPage } from "../MainPage";
import { type Page } from '@playwright/test';

export class IdentityOverviewPage extends MainPage {
    
    constructor(page: Page) {
        super(page);
    }

    
}