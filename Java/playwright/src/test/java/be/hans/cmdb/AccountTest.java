package be.hans.cmdb;

//import be.cmdb.helpers.AccountHelper;
//import be.cmdb.model.Account;
//import be.cmdb.pages.Account.AccountDetailsPage;
//import be.cmdb.pages.Account.AccountOverviewPage;
//import be.cmdb.pages.Account.CreateAccountPage;
//import be.cmdb.pages.MainPage;
import org.junit.jupiter.api.Test;

//import static org.assertj.core.api.Assertions.assertThat;

public class AccountTest extends BaseTest{

    @Test
    void canCreateNewAccount(){
//        Account newaccount = AccountHelper.createRandomAccount(getSession());
//        MainPage mainPage = doLogin();
//        AccountOverviewPage overviewPage = mainPage.openAccountOverview();
//        CreateAccountPage createAccountPage = overviewPage.openCreateAccount();
//        createAccountPage.setUserId(newaccount.getUserId());
//        createAccountPage.selectType(String.valueOf(newaccount.getTypeId()));
//        createAccountPage.selectApplication(String.valueOf(newaccount.getApplicationId()));
//        createAccountPage.clickCreateButton();
//        //Verify that the account was created successfully
//        overviewPage.search(newaccount.getUserId());
//        AccountDetailsPage detailsPage =  overviewPage.openDetailPage();
//        String logline = detailsPage.getLastLogline();
//        //assertThat(logline).contains("table account")
//        //    .contains(newaccount.getUserId())
//        //    .contains("by "+getAccount().getUserId());
    }
}
