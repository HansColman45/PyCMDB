@model IEnumerable<CMDB.Domain.Entities.Permission>
@{
    Layout = "_Layout";
}
<h2>@ViewData["Title"]</h2>
<partial name="_Search" />
@if (Model.Count() > 0)
{

}
else
{
    <div class="alert alert-danger">No rows found, please add a new record</div>
}