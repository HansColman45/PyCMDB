@{
    Layout = "_Layout";
    Identity identity = (Identity)ViewBag.Identity;
    Account account = (Account)ViewBag.Account;
}
@if ((bool)ViewData["ReleaseAccount"])
{
    <h2>@ViewData["Title"]</h2>
    <table class="table table-striped table-bordered">
        <thead class="thead-light">
            <tr>
                <th>Name</th>
                <th>UserID</th>
                <th>E Mail</th>
                <th>Language</th>
                <th>Type</th>
                <th>Active</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@identity.Name</td>
                <td>@identity.UserID</td>
                <td>@identity.EMail</td>
                <td>@identity.Language.Description</td>
                <td>@identity.Type.Description</td>
                <td>@identity.Active</td>
            </tr>
        </tbody>
    </table>
    <H3>Account information that will be released</H3>
    <table class="table table-striped table-bordered">
        <thead class="thead-light">
            <tr>
                <th>UserID</th>
                <th>Type</th>
                <th>Application</th>
                <th>Active</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@account.UserID</td>
                <td>@account.Type.Type</td>
                <td>@account.Application.Name</td>
                <td>@account.Active</td>
            </tr>
        </tbody>
    </table>
    <partial name="_SignForm" />
}
else
{
    <p>You do not access to this page</p>
}
